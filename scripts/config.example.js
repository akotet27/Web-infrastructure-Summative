/* Copy this file to scripts/config.js and add your openFDA API key.
   scripts/config.js is git-ignored so the key never reaches the repo.
   The app also works with no key at all (lower shared rate limit).
   Get a free key: https://open.fda.gov/apis/authentication/ */
window.NEUROREF_CONFIG = {
  apiKey: '', // e.g. 'AbCdEfGh123...'
};
